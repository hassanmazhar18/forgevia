FORGEVIA - RUN ON YOUR OWN PC (FREE, NO CARD, NO ACCOUNT)

1. Extract this zip anywhere (e.g. C:\forgevia)
2. Open the "windows" folder and double-click START_FORGEVIA.bat
3. First run installs requirements (~1 min). Then it prints:
      Public URL : https://something.trycloudflare.com
   That is your live website address. Share it. Your browser opens it automatically.
4. Keep the black window open. Closing it takes the site offline; re-run the .bat to go back online
   (data is saved on your PC in the "data" folder - nothing is lost).

Sign in: has2255000@gmail.com + your password  ->  Account -> change password.

NOTES
- The free tunnel address changes each time you restart. For a fixed address, use
  the Render option later (free virtual card) or tell me and I'll set up a free named tunnel.
- Keep your PC from sleeping: Settings > System > Power > Screen and sleep > "Never" (when plugged in).
- Windows Firewall may ask once: click "Allow access".
- Node.js (free, nodejs.org) is optional; it enables JavaScript code running for your users.
